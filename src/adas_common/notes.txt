TODO: 
    - annotate all C++ headers and source. (exclude param stuff and )
    - need to convert all python scripts to python3 (refer: http://wiki.ros.org/noetic/Migration)
    - need to link header libraries and regular libraries properly
    - using boost and eigen

NEED TO RE-ARCHTECTURE NODES, TOPICS, PARAMETERS AND LAUNCH SEQUENCE

TODO: 
    - rewrite lateral_controller source and header using tf2 datatypes

LOOK: 
    - tf2 transform datatypes function to transform datatypes to Eigen datatypes

    annotate all of adas_commons and compile correctly
    port some downstream packages
    commit and push git

use tf_<conversion> packages to transform between message objects

Martin: Eigen resolution (pure pursuit package)
        ros2 library package -> look at rpi repo PRs (regular and header only -> need both for this) and threadsafe_queue package
        boost resolution

        TODO MONDAY: figure out boost, header only and normal library cmakelists
                port car packages

#cmakelists template
set (HEADER_FILES include/threadsafe_queue/threadsafe_queue.hpp)
add_library(threadsafe_queue_lib src/threadsafe_queue.cpp ${HEADER_FILES})

target_include_directories(threadsafe_queue_lib
  PUBLIC
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>)

ament_export_targets(threadsafe_queue_lib HAS_LIBRARY_TARGET)

install(
  DIRECTORY include/threadsafe_queue
  DESTINATION include
)

install(
  TARGETS threadsafe_queue_lib
  EXPORT threadsafe_queue_lib
  LIBRARY DESTINATION lib
  ARCHIVE DESTINATION lib
  RUNTIME DESTINATION bin
  INCLUDES DESTINATION include
)